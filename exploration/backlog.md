# Renderer Backlog

Items confirmed against `plugins/small-claims-assistant/assets/example/example1-missing-user-address.pdf` as the reference target.

---

## START HERE — PDF Renderer: Match Example Output

**Priority:** High
**Evidence:** `example1-missing-user-address.pdf` (5 pages, BC Filing Assistant output)
**Current state:** Our renderer produces 4 pages with partial field population and layout misalignment on page 3.

### Task 1 — Fix page 3 layout

| Issue | Current | Target |
|---|---|---|
| Claimant block | Name + address | Name only (Form 38 carries address) |
| WHAT HAPPENED? | Full facts text stamped inline | "See attached pages." |
| WHERE | "Victoria, BC, CA" | "Victoria / BC" (city + province, no country) |
| WHEN | ISO date range string | "April 2026" (Month YYYY) |
| Remedy rows | Misaligned, descriptions bleed into WHERE/WHEN | Short labels, rows a–e, properly aligned |
| Filing fees | Not rendered | $100 (≤$3,000 claim) or $156 (>$3,000) |
| Court address | Not rendered | Derived from registry location (e.g. "Victoria Law Courts, 850 Burdett Avenue...") |

### Task 2 — Generate attachment page dynamically

The attachment page is NOT part of the base template — it must be generated by the renderer and inserted before the Certificate of Service page.

Structure (driven by `claim.category`):
- Heading: "What Happened? - [category label]" (e.g. "Defective Goods / Services")
- Structured Q&A fields (see Task 3 for required JSON)
- BC government branding header

Output: 5-page package (cover + instructions + notice form + attachment + certificate of service)

### Task 3 — Extend canonical JSON schema for structured attachment fields

The attachment page requires structured fields not yet in the JSON. Required additions under `claim.structured` for category `goods-or-services`:

- `agreementInWriting` (bool)
- `amountAgreed` (float)
- `dateAgreed` / `datePurchased` (date)
- `amountPaid` (float)
- `datePaid` (date)
- `whatWentWrong` (string)
- `defendantAttemptedCorrection` (bool)
- `replacementObtained` (bool)

Intake agent must be updated to collect these fields.

### Task 4 — Certificate of Service: pre-fill Registry Location

Page 5 (Certificate of Service) should have `jurisdiction.registryLocation` pre-filled in the REGISTRY LOCATION box. All other fields remain blank (completed by process server after filing).

---

## Notes

- `example1-missing-user-address.pdf` is the authoritative reference for page layout and structure.
- Claimant address intentionally absent in example — Form 38 is the address-of-service carrier, not the Notice of Claim.
- Filing fee is deterministic from total claim amount: $100 (≤$3,000) or $156 (>$3,000).
- Court address is derivable from `jurisdiction.registryLocation` via a registry lookup table in the renderer.
